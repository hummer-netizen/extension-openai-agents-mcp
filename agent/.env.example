# OpenAI API key (required)
OPENAI_API_KEY=sk-your-key-here

# Webfuse Session MCP endpoint (optional, defaults shown)
MCP_BASE_URL=https://session-mcp.webfu.se/mcp

# Server config (optional)
HOST=0.0.0.0
PORT=8000
