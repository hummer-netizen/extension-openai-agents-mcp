// content.js — Minimal content script
// MCP handles all page interaction through the Webfuse Session MCP server.
// This script only logs that the extension is active.

console.log("[OpenAI Agent] Webfuse extension loaded. MCP tools handle page interaction.");
